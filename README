                                  ur
                    make x86 assembler unreliable

SYNOPSIS
    ur [OPTION]... [INPUT-ASM-FILE]

DESCRIPTION
    Transforms x86 assembler replacing specified instructions (e.g.,
    cmpl) with unreliable counterparts which do the wrong thing with a
    specified probability.

EXAMPLE
    $ gcc -S etc/printer.c -o -|./ur|gcc -x assembler - -o etc/printer-ur
    $ for i in $(seq 300);do ./etc/printer-ur; sleep 1;done
